EXPO_PUBLIC_API_MODE=real
# Use the Mac LAN IP for physical iPhone testing, not localhost.
EXPO_PUBLIC_API_URL=http://<LAN_IP>:8001/api/v1

# Optional local demo mode for app-only UI work:
# EXPO_PUBLIC_API_MODE=mock
# EXPO_PUBLIC_API_URL=
